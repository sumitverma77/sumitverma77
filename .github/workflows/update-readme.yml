name: 🤖 Update GitHub Stats README

on:
  schedule:
    # Run daily at 00:30 UTC (06:00 IST)
    - cron: "30 0 * * *"
  workflow_dispatch: # Allow manual trigger from Actions UI

jobs:
  update-readme:
    name: Fetch GitHub Stats & Update README
    runs-on: ubuntu-latest
    permissions:
      contents: write  # Required to push the updated README

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          # Uses the built-in GITHUB_TOKEN — no manual PAT required
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 1

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🚀 Run README bot
        env:
          # Built-in token — automatically scoped to this repo, no setup needed
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_USERNAME: sumitverma77
        run: |
          echo "Starting README automation..."
          START_TIME=$(date +%s%3N)
          node scripts/index.js
          END_TIME=$(date +%s%3N)
          echo "Execution time: $((END_TIME - START_TIME))ms"

      - name: 💾 Commit & push if changes exist
        run: |
          git config user.name "NeonPanda[bot]"
          git config user.email "NeonPanda@users.noreply.github.com"
          # Safe commit pattern: only push if there are actual diffs or untracked files
          git add README.md leetcode-stats.svg github-stats.svg || true
          if git diff --staged --quiet; then
            echo "✅ No changes detected — Assets are already up-to-date."
          else
            git commit -m "chore(readme): auto-update GitHub stats & LeetCode Cyberpunk SVG [skip ci]"
            git push
            echo "✅ README.md and/or SVG updated and pushed successfully."
          fi
